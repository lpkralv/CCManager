---
id: REQ-017
title: Display task output back to user in CCManager
status: completed
created_at: 2026-02-05T18:55:00Z
claimed_at: 2026-02-05T20:11:00Z
route: B
completed_at: 2026-02-05T20:18:00Z
---

# Display Task Output Back to User

## What
When a prompt dispatched to a project generates output (e.g., an answer to a question, a summary, or any textual response), that output should be visible to the user within CCManager. The task history is one possible location, but the implementation may use a better approach if one exists.

## Context
Currently tasks can be dispatched and their status is tracked, but the actual output/response from Claude Code may not be surfaced clearly to the user. The user needs to see what came back — especially for prompts that ask questions rather than perform actions. This could be shown in the task history detail view, a dedicated output panel, or a popup when a task completes.

---
*Source: "When the prompt sent to a project generates an output (e.g., when the user asks a question) that information should come back to the CCManager for the user to see. I presume this would be in the task history, though you may have a better idea."*

---

## Triage

**Route: B** - Medium

**Reasoning:** Clear outcome (display task output) but need to explore how output is captured in process-spawner/task-manager, what data flows through WebSocket, and what the task history UI looks like to determine the best approach.

## Exploration

- **Capture**: process-spawner accumulates stdout in `output` field, emits `output` events — COMPLETE
- **Storage**: task-manager stores output on task object, history-service persists full task with output — COMPLETE
- **Transmission**: WebSocket broadcasts `task:output` events to all clients — COMPLETE
- **Frontend receive**: app.js accumulates output on activeTasks, moves full task to history — COMPLETE
- **Display gap**: Active Tasks pane shows output for running tasks (`<pre>` tag). Task History pane does NOT show output — only metadata (project, status, prompt, timing)
- **Approach**: Add "View Output" capability to Task History items — truncated preview + expandable/modal view for full output

*Generated by Explore agent*

## Implementation Summary

- Added "View Output" button on Task History items (visible when task has output) in `public/index.html`
- Added Task Output modal (`modal-large`) with: project name, status badge, duration, prompt, scrollable output `<pre>`, and error section (red-tinted)
- Added `showTaskOutput`, `selectedTaskOutput` state and `viewTaskOutput()` method in `public/js/app.js`
- Added auto-scroll for active task output on WebSocket `task:output` events
- Added CSS for output modal, error section, and view-output button in `public/css/styles.css`
- All 88 tests passing

*Completed by work action (Route B)*

## Testing

**Tests run:** `npx vitest run`
**Result:** 9 files, 88 tests, all passing

*Verified by work action*
