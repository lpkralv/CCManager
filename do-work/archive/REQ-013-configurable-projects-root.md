---
id: REQ-013
title: Configurable projects root directory for portability
status: completed
created_at: 2026-02-05T18:50:00Z
claimed_at: 2026-02-05T19:13:00Z
route: C
completed_at: 2026-02-05T19:25:00Z
---

# Configurable Projects Root Directory

## What
Add a configuration capability so the user can specify where the folder containing Claude Code project folders lives. This is needed to make the Mac app portable across different Macs and different users on the same Mac.

## Context
Currently `PROJECTS_ROOT` is set via `.env` file, which is tied to the development environment. For a distributed Mac app, there needs to be a user-facing way to configure this path — either through a settings UI within the app, a first-run setup prompt, or a preferences dialog. The path will differ per machine/user (e.g. `/Volumes/Sheridan/sandbox/` vs `/Users/someone/projects/`).

---
*Source: "I want to be able to move the Mac app to other Macs and other users on this Mac. I believe I will need to have some sort of configuration capability to identify where the folder containing Claude Code project folders lives."*

---

## Triage

**Route: C** - Complex

**Reasoning:** Multi-system feature requiring: settings persistence (can't use .env for distributed app), settings UI in the dashboard, server-side config loading, API endpoint for reading/writing settings, and integration with existing project discovery. Touches backend, frontend, and potentially Electron layers.

## Plan

1. Create `src/services/settings-service.ts` — reads/writes `data/settings.json`, exports `loadSettings()`, `saveSettings()`, `getProjectsRoot()`, `setProjectsRoot()`. Env var `PROJECTS_ROOT` takes precedence as fallback.
2. Create `src/server/routes/settings.ts` — `GET /api/settings` and `PUT /api/settings` with zod validation and directory existence check.
3. Register settings route in `src/server/routes/index.ts`.
4. Update `src/services/project-creator.ts` — replace hardcoded `SANDBOX_ROOT` with dynamic `getProjectsRoot()` call.
5. Add settings UI: settings button in header, settings modal with projectsRoot input, first-run setup banner when not configured.
6. Add Alpine.js settings state and methods (`loadSettings`, `saveSettings`, `needsSetup`).
7. Add CSS for setup banner, form hints, x-cloak.
8. Add `data/settings.json` to `.gitignore`.

*Generated by Plan agent*

## Exploration

- **Route registration**: `src/server/routes/index.ts` — simple `router.use("/settings", settingsRouter)` pattern
- **JSON data service pattern**: `src/services/history-service.ts` — `DATA_DIR = path.join(process.cwd(), "data")`, `ensureDataDir()`, `loadHistory()`/`saveHistory()` functions
- **Hardcoded SANDBOX_ROOT**: `src/services/project-creator.ts` lines 10-11 — used at line 189 (project path) and line 197 (CCSTARTUP check)
- **Barrel exports**: `src/services/index.ts` exists, add export line
- **HTML modals**: Follow `.modal-backdrop` + `.modal` + `.modal-header` + `.modal-actions` pattern (see New Project modal at lines 295-329)
- **Alpine.js pattern**: Boolean flags for modal visibility (`showNewProject`, `showProjectDetails`), async methods with try/catch, `fetch()` API calls
- **CSS**: Modal classes at lines 404-454, form styles at lines 220-256, use existing CSS variables
- **Header**: Settings button goes in `.status-bar` div near "Stop Server" button
- **First-run banner**: Place after `<main>` before `.summary-container`, show when settings not configured

*Generated by Explore agent*

## Implementation Summary

- **Created** `src/services/settings-service.ts` — settings read/write service with zod schema, `getProjectsRoot()` (env override → settings → undefined), `getProjectsRootSource()`
- **Created** `src/server/routes/settings.ts` — `GET /api/settings` and `PUT /api/settings` with directory existence validation
- **Modified** `src/server/routes/index.ts` — registered settings router
- **Modified** `src/services/index.ts` — added barrel export
- **Modified** `src/services/project-creator.ts` — removed hardcoded `SANDBOX_ROOT`, now uses dynamic `getProjectsRoot()` with error if not configured
- **Modified** `public/index.html` — settings button in header, settings modal, first-run setup banner with `x-cloak`
- **Modified** `public/js/app.js` — settings state, `loadSettings()`/`saveSettings()` methods, integrated into `init()`
- **Modified** `public/css/styles.css` — setup banner, form-hint, x-cloak styles
- **Modified** `.gitignore` — added `data/settings.json`
- TypeScript build succeeded with no errors

*Completed by work action (Route C)*

## Testing

**Tests run:** No testing infrastructure detected
**Result:** N/A — TypeScript compilation verified clean

*Verified by work action*
