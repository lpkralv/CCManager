---
id: REQ-026
title: Implement launchd service for auto-starting server
status: completed
created_at: 2026-02-05T19:06:00Z
claimed_at: 2026-02-05T22:03:00Z
route: B
completed_at: 2026-02-05T22:13:00Z
related: [REQ-024, REQ-025]
---

# Implement launchd Service for Auto-Starting Server

## What
Implement Option 2 from REQ-024: a macOS `launchd` service that starts the CCManager server automatically at login, so the web app is always available at `http://localhost:3000` without manual server start.

## Context
Per REQ-024 analysis, the `launchd` approach is the best fit for the browser-based workflow, especially in the context of REQ-025 (cross-user execution from the louisslothouber account). It needs:
- A `~/Library/LaunchAgents/com.lpkralv.ccmanager.plist` file
- Configured to start the Node.js server at login
- Log output to a file
- A one-time setup script or instructions for `launchctl load`
- Consider which user account this runs under (may tie into REQ-025 findings)

---
*Source: "I will use the finding from REQ-025. But that means I need to implement one of the options in REQ-024 to quickly launch the server. Which do you recommend? Implement that one."*

---

## Triage

**Route: B** - Medium

**Reasoning:** Clear deliverable but need to verify Node.js path, server entry point, and launchd plist format.

## Exploration

- Node.js at `/usr/local/bin/node` v22.19.0
- Compiled server at `dist/server/index.js`
- Current user: `agenthome`
- Server binds to port 3000 by default

*Generated by Explore agent*

## Implementation Summary

- Created `scripts/launchd/com.lpkralv.ccmanager.plist` — launchd agent config with RunAtLoad, KeepAlive=false, logs to `logs/` directory
- Created `scripts/launchd/setup.sh` — installs plist to `~/Library/LaunchAgents/`, loads service, verifies health endpoint
- Created `scripts/launchd/teardown.sh` — unloads service, removes plist
- Added npm scripts: `service:install`, `service:uninstall`, `service:status`, `service:logs`
- Added `logs/` to `.gitignore`
- All 89 tests passing

*Completed by work action (Route B)*

## Testing

**Tests run:** `npx vitest run`
**Result:** 9 files, 89 tests, all passing

*Verified by work action*
