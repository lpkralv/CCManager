---
id: REQ-003
title: Multi-project selection and dispatch
status: completed
created_at: 2026-02-03T12:10:00Z
claimed_at: 2026-02-03T19:00:00Z
route: C
completed_at: 2026-02-03T19:15:00Z
---

# Multi-Project Selection and Dispatch

## What
Add the ability to select multiple projects in the PROJECTS pane and dispatch the same task to all selected projects simultaneously.

## Detailed Requirements

### Selection Behavior
- Support SHIFT modifier key for range selection (select all projects between last selected and clicked)
- Support COMMAND (Mac) / CTRL (Windows/Linux) modifier key for toggle selection (add/remove individual projects from selection)
- Visual indication of selected projects (highlight, checkbox, or similar)
- Clicking without modifier clears previous selection and selects only the clicked project (current behavior)

### DISPATCH TASK Pane Updates
- When multiple projects are selected, display all project names in the "Project" field
- Consider how to display multiple projects (comma-separated list, count indicator, expandable list, etc.)
- Prompt textarea remains the same - single prompt for all selected projects

### Dispatch Behavior
- When DISPATCH TASK is pressed with multiple projects selected, send the prompt to ALL selected projects simultaneously
- Each project gets its own independent task execution
- Task status should be tracked independently for each dispatched task

## Context
- This enables batch operations across the project portfolio
- Useful for running the same maintenance task, update, or query across multiple projects
- Complex feature requiring planning due to UI state management and parallel dispatch logic

## Requirements
1. Plan the implementation (UI changes, state management, dispatch logic)
2. Verify the plan covers all requirements
3. Update plan if needed
4. Implement the verified plan

---
*Source: Sometimes I would like to be able to dispatch tasks to ALL projects or a subset of projects. Allow the use of the SHIFT and COMMAND/CTRL modifier keys when clicking on projects in the PROJECTS pane, to select multiple projects simultaneously. When this is done, add all project names to the "Project" entry in the DISPATCH TASK Pane. The prompt in that pane should then be sent to all the projects that were selected, simultaneously when DISPATCH TASK is pressed. This is a complex feature add, so planning will be needed. Build the plan, Verify the plan and update if needed. Then implement the verified plan.*

---

## Triage

**Route: C** - Complex

**Reasoning:** New feature requiring UI state management (multi-select), keyboard modifier handling, visual updates, and parallel dispatch logic. Touches frontend extensively and requires architectural decisions.

## Plan

### Architecture Decisions

1. **Selection State:** Use `selectedProjects: []` array (works well with Alpine.js reactivity)
2. **Backend Changes:** None needed - frontend dispatches parallel POST requests
3. **Track `lastSelectedIndex`** for SHIFT+click range selection

### Implementation Steps

1. **Update State** (`public/js/app.js`):
   - Replace `selectedProject: null` with `selectedProjects: []`
   - Add `lastSelectedIndex: null`

2. **Create Selection Handler** (`handleProjectClick(project, event)`):
   - Plain click: Clear selection, select single project
   - SHIFT+click: Select range from lastSelectedIndex to current
   - CMD/CTRL+click: Toggle project in selection

3. **Add Helper Methods**:
   - `isProjectSelected(project)` - check if project is in selection
   - `getSelectedProjectNames()` - adaptive display (names or "N projects selected")
   - `hasSelection()` - check if any projects selected

4. **Update `dispatchTask()`**:
   - Map over `selectedProjects`
   - Use `Promise.allSettled()` for parallel dispatch
   - Handle partial failures

5. **Update HTML** (`public/index.html`):
   - Project items: `@click="handleProjectClick(project, $event)"`
   - Add `:class="{ 'selected': isProjectSelected(project) }"`
   - Update dispatch form to show multiple project names
   - Dynamic labels: "Project(s)", "Dispatch Task(s)"

6. **Update CSS** (`public/css/styles.css`):
   - Add selection indicator styles
   - Optional: hint text for keyboard shortcuts

### Files to Modify

| File | Changes |
|------|---------|
| `public/js/app.js` | State, selection handler, helpers, dispatch logic |
| `public/index.html` | Click handlers, bindings, conditional text |
| `public/css/styles.css` | Multi-selection visual styles |

*Generated by Plan agent*

## Exploration

### Current Implementation

**State (`public/js/app.js:9`):**
- Uses `selectedProject: null` (single project)
- `selectProject(project)` simply assigns clicked project

**HTML (`public/index.html`):**
- Line 44: `:class="{ 'selected': selectedProject?.id === project.id }"`
- Line 45: `@click="selectProject(project)"` (no event param)
- Line 66: Shows `selectedProject?.name`

**CSS (`public/css/styles.css:146-149`):**
- Already has `.project-item.selected` with red accent styling

### Patterns to Follow

- Selection state: Replace single object with array
- Click handler: Add `$event` parameter for modifier detection
- Conditional class: Use helper method `isProjectSelected(project)`
- Form display: Use adaptive `getSelectedProjectNames()`

*Generated by Explore agent*

## Implementation Summary

Modified `public/js/app.js`:
- Replaced `selectedProject: null` with `selectedProjects: []` and `lastSelectedIndex: null`
- Added `handleProjectClick(project, event)` with SHIFT/CMD/CTRL modifier support
- Added helpers: `isProjectSelected()`, `getSelectedProjectNames()`, `hasSelection()`
- Updated `dispatchTask()` to use `Promise.allSettled()` for parallel dispatch
- Updated `createProject()` to select newly created project

Modified `public/index.html`:
- Changed click handler to `@click="handleProjectClick(project, $event)"`
- Changed selection binding to `:class="{ 'selected': isProjectSelected(project) }"`
- Updated form to use `hasSelection()` and `getSelectedProjectNames()`
- Added dynamic "Project(s)" label

*Completed by work action (Route C)*

## Testing

**Tests run:** npm run typecheck
**Result:** ✓ TypeScript compilation successful

**Manual verification needed:**
- Plain click: selects single project
- SHIFT+click: range selection
- CMD/CTRL+click: toggle selection
- Multi-project dispatch creates independent tasks

*Verified by work action*
