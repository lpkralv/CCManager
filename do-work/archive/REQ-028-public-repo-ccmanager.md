---
id: REQ-028
title: Create clean public GitHub repo for CCManager
status: completed
created_at: 2026-02-05T19:10:00Z
claimed_at: 2026-02-05T22:20:00Z
route: C
completed_at: 2026-02-05T22:30:00Z
---

# Create Clean Public GitHub Repo for CCManager

## What
Create a new public GitHub repository named "CCManager" containing only the clean end product of this project. Strip out all development artifacts, internal tooling, and AI-assistant scaffolding.

## Must exclude
- `do-work/` directory (all request files, archives, working, assets)
- Local `.git/` history (fresh repo, no development commit history)
- `/init` support files and scaffolding
- `CLAUDE.md`
- `.claude/` directory (MCP config, settings, etc.)
- Any other Claude Code-specific configuration or artifacts

## Must include
- Clean source code (`src/`)
- Built output or build instructions
- `package.json` (cleaned of any dev-only scripts that reference internal tooling)
- Proper public-facing `README.md` with setup/usage instructions
- The Electron app configuration and build scripts
- Public assets, CSS, HTML
- A fresh git history (single initial commit or clean set of commits)

## Context
The repo name should be "CCManager" under the user's GitHub account (lpkralv). This is a distribution-ready version — think of it as a release export, not a fork. A `.gitignore` for the new repo should be tailored to exclude development artifacts if any slip through.

---
*Source: "I want to make this project public. That is, I want to create a new GitHub public repo for the end product of this project. I DO NOT want any of the historical development artifacts to be in that public repo (i.e., no do-work directory, no local GIT repo, none of the custom /init support, no Claude support (e.g., CLAUDE.md, .claude/). The name of this repo should be CCManager."*

---

## Triage

**Route: C** - Complex

**Reasoning:** Multi-phase operation: create clean copy, modify files, write README, create GitHub repo, push. Requires planning and careful file filtering.

## Plan

1. Create temp working directory with fresh git init
2. Copy clean source files (src/, public/, scripts/, config files)
3. Create seed data files (empty inventory.json, task-history.json)
4. Modify package.json (name, repository, homepage fields)
5. Create path-agnostic start_CCManager.command
6. Create public README.md with features, setup, usage
7. Create MIT LICENSE file
8. Create tailored .gitignore and .env.example
9. Update launchd setup.sh to generate plist dynamically
10. Run tests to verify clean copy works
11. Create public GitHub repo via `gh repo create`
12. Push with single initial commit

*Generated by Plan agent*

## Implementation Summary

- Created clean copy in temp directory, excluding: do-work/, init/, background/, .claude/, .serena/, .mcp.json, personal data files
- Copied: src/, public/, scripts/, config files, test infrastructure
- Created seed data files (empty inventory.json, task-history.json)
- Modified package.json (name: ccmanager, added repository/homepage fields)
- Created path-agnostic start_CCManager.command (auto-detects project dir)
- Updated launchd setup.sh to generate plist dynamically (no hardcoded paths)
- Fixed Electron "Learn More" URL to point to CCManager repo
- Created MIT LICENSE, .env.example, tailored .gitignore
- Created comprehensive public README.md
- Verified: typecheck passed, 89 tests passed, build succeeded
- Created public repo: https://github.com/lpkralv/CCManager
- Pushed single initial commit with 56 files
- Added topics: claude-code, dashboard, typescript, electron
- Source project NOT modified

*Completed by work action (Route C)*

## Testing

**Tests run:** `npm test` (in clean copy)
**Result:** 9 files, 89 tests, all passing
**Typecheck:** `npm run typecheck` — zero errors
**Build:** `npm run build` — compiled successfully

*Verified by work action*
