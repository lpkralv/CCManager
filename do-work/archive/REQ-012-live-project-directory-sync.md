---
id: REQ-012
title: Live sync of project directory changes to Projects pane
status: completed
created_at: 2026-02-05T18:49:00Z
claimed_at: 2026-02-05T19:06:00Z
route: B
completed_at: 2026-02-05T19:12:00Z
---

# Live Project Directory Sync

## What
Confirm that live changes to the directory containing the Claude Code projects (adding, removing, or renaming project folders) are reflected in the Projects pane list without requiring a manual refresh or server restart.

## Context
The projects are discovered from a root directory (configured via `PROJECTS_ROOT`). If a user adds or removes a project folder on disk, the Projects pane should pick up the change automatically or on next load. This may require filesystem watching or periodic polling on the server side, or simply re-fetching the project list on the client side.

---
*Source: "confirm that live changes to the directory containing the claude code projects are reflected in the PROJECTS pane list"*

---

## Triage

**Route: B** - Medium

**Reasoning:** Clear outcome (live-refresh project list on directory changes) but need to explore how projects are currently discovered, whether the service reads from disk on each call or caches, and what client-side refresh patterns exist.

## Exploration

- **Project discovery**: Projects stored in `data/inventory.json`, loaded via `getAllProjects()` in `project-service.ts` — reads JSON on every call (no cache), but no filesystem scanning
- **No live refresh**: Frontend loads projects once on `init()`, no polling, no auto-refresh
- **No filesystem watching**: No `fs.watch()` or chokidar; `PROJECTS_ROOT` is defined in `.env.example` but not used in any TypeScript code
- **WebSocket**: Only broadcasts task events, no project list events
- **Client-side**: `loadProjects()` fetches from `/api/projects`, result stored in `projects[]` Alpine state; also `loadProjectsSummary()` fetches `/api/projects/summary`
- **Approach**: Add client-side polling (setInterval every 30s for both project list and summary), plus optional manual refresh button. Since the API already reads from disk on each call, server changes are minimal.

*Generated by Explore agent*

## Implementation Summary

- Added `refreshProjects()` method in `public/js/app.js` — refreshes both project list and summary table in parallel
- Added 30-second polling interval in `init()` via `setInterval`
- Added `refreshProjects()` call on WebSocket `task:completed` and `task:failed` events (captures git status changes from task execution)
- Added refresh button (↻) next to "Projects" header in sidebar (`public/index.html`)
- Added `.btn-refresh` and `.card-header-actions` CSS styles (`public/css/styles.css`)
- No server-side changes needed — API already reads fresh data on each call
- TypeScript build succeeded

*Completed by work action (Route B)*

## Testing

**Tests run:** No testing infrastructure detected
**Result:** N/A — TypeScript compilation verified clean

*Verified by work action*
