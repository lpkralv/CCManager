---
id: REQ-018
title: Ensure Mac and Web app feature parity
status: completed
created_at: 2026-02-05T18:56:00Z
claimed_at: 2026-02-05T20:19:00Z
route: B
completed_at: 2026-02-05T20:25:00Z
---

# Mac and Web App Feature Parity

## What
Ensure that the Mac (Electron) app and the web app have identical functionality. All features available in one should work the same way in the other.

## Context
Both apps share the same frontend (served from the Express server), so they should already be largely in sync. However, differences could arise from port configuration (web on 3000, Electron on 3001), Electron-specific behaviors, or features that were only tested in one context. This request is to verify and fix any discrepancies so both apps behave identically.

---
*Source: "make sure that the functionality of the Mac and Web apps are in sync"*

---

## Triage

**Route: B** - Medium

**Reasoning:** Verification/audit task — need to explore Electron main process vs web server setup, check for hardcoded ports or URLs in frontend, and identify any behavioral differences.

## Exploration

- **Frontend parity**: PERFECT — all API calls use relative paths, WebSocket uses `window.location.host`, no hardcoded URLs/ports
- **Server code**: Shared via `src/server/app.ts` — identical for both
- **CSS/HTML/JS**: Shared via Express static serving — identical
- **Acceptable differences**: Electron has native menu bar, window controls, DevTools auto-open (platform-specific, expected)
- **Issue found**: Shutdown behavior — clicking "Stop Server" in Electron kills the server but leaves the window open and non-functional. The `serverProcess.on("exit")` handler only logs, doesn't take action.
- **Fix**: Detect server exit in Electron and either quit the app or show a restart dialog

*Generated by Explore agent*

## Implementation Summary

- **Audit result**: 99% parity — frontend, API, WebSocket, CSS all shared identically
- **One fix applied**: Electron shutdown behavior in `src/electron/main.ts`
  - Added `appInitiatedShutdown` flag to distinguish user quit vs server stop
  - Enhanced `serverProcess.on("exit")` to call `app.quit()` when server exits (prevents dead window)
  - Added `dialog.showErrorBox()` for unexpected server crashes
  - Updated `stopServer()` to set flag before killing process
- All 88 tests passing

*Completed by work action (Route B)*

## Testing

**Tests run:** `npx vitest run`
**Result:** 9 files, 88 tests, all passing

*Verified by work action*
