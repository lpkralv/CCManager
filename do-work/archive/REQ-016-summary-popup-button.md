---
id: REQ-016
title: Summary as popup window with header button
status: completed
created_at: 2026-02-05T18:54:00Z
related: [REQ-011]
claimed_at: 2026-02-05T20:03:00Z
route: B
completed_at: 2026-02-05T20:10:00Z
---

# Summary as Popup Window with Header Button

## What
Change the startup status summary table (from REQ-011) so that it is displayed in a pop-up/modal window rather than as a pane in the dashboard. Add a "Summary" button in the header bar (alongside "Stop Server" and "Configure") so the user can open it at any time, not just on startup.

## Context
The status summary should not take up permanent screen real estate on the dashboard. It should be a closeable popup/modal that the user can dismiss after reviewing and re-open whenever needed via the header button. This replaces the original REQ-011 approach of showing it as a startup landing view.

---
*Source: "the startup status list should be displayed in a pop-up window, not as a pane in the dashboard. We want to be able to close it after reviewing the summary info. It should not take up screen real estate on the dashboard. Ideally, rather than opening it on startup, there would be another button on the top-line (on par with the 'Stop Server' and 'Configure' buttons) for 'Summary', so that we can review it at any time."*

---

## Triage

**Route: B** - Medium

**Reasoning:** Clear outcome (move summary into modal, add header button) but need to find the exact HTML of the existing summary table and modal patterns to restructure.

## Exploration

- **Summary table**: Lines 45-93 in `public/index.html`, `.summary-container` with `.summary-card` and `.summary-table`
- **Header buttons**: Lines 25-30 — "Configure" and "Stop Server" buttons in `.status-bar`
- **Modal pattern**: Settings modal (lines 343-374) uses `.modal-backdrop` + `.modal` + `.modal-header` with close button
- **Alpine.js state**: `projectsSummary[]` and `loadingSummary` already exist; need to add `showSummary: false`
- **Data loading**: `loadProjectsSummary()` already loads on init — modal will have fresh data

*Generated by Explore agent*

## Implementation Summary

- Moved summary table from inline dashboard section into a `modal-large` popup in `public/index.html`
- Added "Summary" button in header `.status-bar` (before Configure and Stop Server)
- Button fetches fresh data on click via `loadProjectsSummary()`
- Removed auto-load of summary on startup — data only fetched when user clicks Summary
- Summary row click now closes summary modal before opening project details
- Polling only refreshes summary data when modal is open
- Added `showSummary: false` to Alpine.js state
- All 88 tests still passing

*Completed by work action (Route B)*

## Testing

**Tests run:** `npx vitest run`
**Result:** 9 files, 88 tests, all passing

*Verified by work action*
